# API server — use your computer's LAN IP when testing on a physical phone
# (localhost only works in iOS simulator / web). Example:
# EXPO_PUBLIC_API_URL=http://192.168.1.42:3000
EXPO_PUBLIC_API_URL=http://localhost:3000
